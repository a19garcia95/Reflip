<div class="container">
  <div class="jumbotron">

    <p id="notice"><%= notice %></p>

    <h1>Extensive Property Research</h1>

    <table class="table">
      <thead>
        <tr>
          <th>Title</th>
          <th>Price</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @researches.each do |research| %>
          <tr>
            <td><%= research.title %></td>
            <td><%= research.price/100 %></td>
            <td><%= link_to 'Show', research %></td>
            <!-- <td><%= link_to 'Edit', edit_research_path(research) %></td>
            <td><%= link_to 'Destroy', research, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'New Research', new_research_path %>
  </div>
</div>
